version: '3.4'
services:
    apache:
        restart: always
        ports:
            - 80:80443
            - 443:443

#    nginx:
#        restart: always
#        ports:
#            - 80:80443
#            - 443:443

    database:
        restart: always

    php-fpm:
        restart: always

    php-cli:
        restart: always

networks:
    app:
